<Window x:Class="Photo_Manager.Views.ErrorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Photo_Manager.Views"
        mc:Ignorable="d"
        Title="Error" Height="350" Width="600"
        WindowStyle="ToolWindow"
        WindowStartupLocation="CenterScreen">
    <Grid x:Name="mainErrorGrid"
          Margin="0,50,0,50">
        <TextBlock VerticalAlignment="Top"
                   HorizontalAlignment="Center"
                   FontSize="20"> Wystąpił błąd </TextBlock>
        <TextBlock x:Name="errortext"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   FontSize="18">Błąd[tekst] </TextBlock>
        <TextBlock VerticalAlignment="Bottom"
                   HorizontalAlignment="Center"
                   FontSize="20"> Spróbuj ponownie</TextBlock>
    </Grid>
</Window>
